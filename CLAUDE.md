# 彤鼎官网项目指令（CLAUDE.md）

本仓库是彤鼎集团（香港）有限公司官方网站（https://tdgroup.hk），托管于 GitHub Pages。你在本仓库中执行任何任务，必须严格遵守本文件的全部规则。

## 零、按需加载索引（⭐ 先读这一节，别一次读完所有规则）

本文件只放**每次会话都用得上的常驻铁律**（品牌、禁用表述、修改边界、分工原则）。
分线细则已拆进 `.claude/rules/`，**内容一字未改，只是搬家**——用到哪条读哪条，不要一次全读。

| 要动什么 | 先读这份 | 体量 |
|---|---|---|
| 写新文章 / 发布上线 / 跑合规闸 | `.claude/rules/publishing.md` | 3.7 KB |
| 初创线（startup-program、两个自测页、初创类文章） | `.claude/rules/line-startup.md` | 3.4 KB |
| AI 转型线（ai-transformation、ai-readiness-checkup） | `.claude/rules/line-ai.md` | 3.2 KB |
| 海外资产线（asset-platform、overseas-asset-listing） | `.claude/rules/line-asset.md` | 2.0 KB |
| 电商税务线（ecommerce-tax-roadmap） | `.claude/rules/line-ecommerce.md` | 1.8 KB |

⛔ **常驻铁律（本文件第二、三、六、八、九节）任何时候都生效**，不因为没读分线文件而豁免。
⛔ 新增业务线时，细则写进 `.claude/rules/` 并在上表加一行；**不要再往本文件里堆**——
   本文件每涨 1 KB，就是每次会话、每个定时班次都多烧一份。

### 会话开场怎么起（省 token 的关键动作）

1. **别怕开新会话**。上下文断了不丢记忆——记忆在仓库文件里，不在聊天记录里。
   同一会话拖得越长，每一轮重发的历史越多，成本是新会话的数倍。
2. **开场先跑** `python tools/session_brief.py "本次要干什么"`，它会告诉你该读哪几份规则、
   当前站点状态、以及上一次会话留下的在办事项。把它的输出当开场白。
3. **搜索一律派给子代理**（`Agent` 工具，`Explore` 类型）。`articles/` 有 260 篇、10 MB，
   主会话直读两三篇就比整份 CLAUDE.md 还贵。子代理烧它自己的上下文、只回结论。
4. **收尾必做**：`python tools/backup.py` 归档会话 + `python tools/handover.py --log "…"` 写工作日志。
   ⚠ 这两步才是"记忆不丢"的真正保险，见第九节。云端容器一回收，没归档的会话永久消失。

## 一、项目结构

- `index.html` 首页 / `about.html` 关于 / `club.html` 俱乐部 / `legacy.html` 传承俱乐部 / `library.html` 文库目录 / `hk-ipo-checkup.html` 港股上市可行性自测 / `financing-program.html` 融资路线图（融资线入口页，2026-09-13 建）
- ⭐ **「服务」下拉菜单只放两组（2026-09-13 廖总「全部按照你说的去改」）**：**融资与上市**（融资路线图 / 初创融资服务 / 上市预备期 / 海外资产上市→`asset-platform.html`）+ **工具**（上市可行性自测 / 上市门槛速查）。首页「服务入口」同样两行。
  ⛔ AI 转型线、电商税务合规**不进顶层导航、不进首页**——页面照旧保留，靠文库、sitemap、llms.txt 与站内互链被找到。理由：品牌是境外上市与跨境资本，导航里混进 2,800 元 AI 落地和核定征收会显得什么都接。`overseas-asset-listing.html` 不单独进导航，由 `asset-platform.html` 顶部按钮进入（同一件事导航里只出现一次）。
  ⚠ 导航写死在 279 个页面里（主页面相对路径、`articles/` 用 `/` 开头），改导航用脚本整块替换 `<div class="mdd-p">…</div></div>`，别逐页手改；分组标题是 `<span class="mdd-h">`，样式在 `css/nav.css`。
- `articles/` 文章全文页（每篇一个 html 文件，英文短横线小写命名，如 `hk-18c-listing.html`）
- `img/` 图片资源 / `sitemap.xml` 站点地图 / `robots.txt`
- 所有页面为纯静态 HTML，禁止引入构建工具、框架或外部依赖。

## 二、品牌标准（逐字使用，禁止改写）

- **对外品牌门面：「TD GROUP 彤鼎」**（2026-09-11 廖总定，英文在前中文在后）。用在 logo、页面标题、og:site_name、Organization 的 name、作者行、封面、名片。
  - 法定名（页脚、落款、合同、schema 的 legalName）：**TD GROUP (HONG KONG) LIMITED** / **彤鼎集團（香港）有限公司**（繁体「團」，照商业登记证逐字）。
  - ⛔ 不许单独用「TD」或「TD集团」做门面，不许提道明 / 加拿大道明 / TD Bank —— 道明在香港已注册「TD」36 类商标（301782469 等 13 条，2026-09-11 知识产权署实查），我们同为金融服务，单用 TD 就是踩它的商标，被误认成道明证券还会碰上《证券及期货条例》114 条（无牌显示自己持牌）。
  - 俱乐部英文名 **Wall Street TD Club**（2026-09-11 廖总定，唯一允许 TD 单独出现的地方）；创始人介绍「曾任职于加拿大券商机构」照写，⛔ 不许补具体券商名。
  - ⛔ 老文章正文里的「彤鼎集团」「彤鼎」不改、分发稿的检索引导句「检索『彤鼎集团』」不改：客户搜「TD集团」出来的是道明，搜「彤鼎」才找到我们，这是搜索资产。
- 公司注册地与网络：于香港注册成立，办公网络：香港·深圳·上海，延伸至新加坡。
- 创始人标准介绍：廖启捷，彤鼎集团（香港）有限公司董事局主席，华尔街彤鼎俱乐部创始人。曾任职于加拿大券商机构，拥有多年北美资本市场交易与投资银行实务经验，并担任港、沪、深三地上市辅导机构的创始合伙人。已主导9家企业完成纳斯达克挂牌辅导，项目覆盖教育、新能源、建材、生物医药、消费等行业。
- **个人业绩与团队业绩必须分开表述（2026-07-25 用户明确）**：
  - **个人口径**：廖启捷本人「九次上市实操」——主导或深度参与九个完整境外及 A 股上市案例。凡描述创始人个人经历，一律用「九次」。
  - **团队口径**：13 个已确认上市案例、成功上市企业市值合计、300+ 已投资及创立公司等，均属**团队及俱乐部核心成员**业绩，必须冠以「彤鼎团队主导及深度参与」或「彤鼎团队及华尔街彤鼎俱乐部核心成员」，**不得写成创始人个人成绩**。
- 八大核心业务（固定顺序）：融资策划、架构设计、上市规划、税务筹划、增发融资、并购重组 / 资产注入、跨境资本、家族办公室。
  ⛔ 第六项 2026-09-04 廖总定，由「市值管理」换成「并购重组 / 资产注入」：证监会《上市公司监管指引第10号》语境里「市值管理」已是负面词，我方也没有券商/财务顾问资质。⛔ 任何**描述我方业务**的地方（业务列表、JSON-LD Service/knowsAbout、时间线、llms.txt 主题、meta description、助手示例）一律不许再出现这四个字；✅ 讲概念、讲合规边界的文章标题与术语解释（market-cap-compliance 等 4 篇、glossary、faq 那条问答）照旧，判定看主语（讲概念放行，只拦「我们做这个」）。
- 对外联系方式：邮箱 liaoqijie@tdgroup.hk（企业邮箱，不得使用 gmail 等个人邮箱）；微信 esonleo；电话 +852 6906 9553；手机 +86 135 7046 1678；地址 香港湾仔港湾道26号华润大厦22楼；及官方社媒账号（知乎「彤鼎ESON廖生」、微博「资助中小企上市-廖生ESON」、百家号/今日头条「彤鼎集团」，公众号/抖音/小红书/视频号以平台后台为准）。⛔ 2026-09-12 廖总定：页脚与创始人页**不再列社媒账号与「全网社媒请搜索」这句**（显得不正式）；页脚只放邮箱、微信；⛔ 电话、地址也不放（廖总 2026-09-12「电话和地址不放」）。
  - 2026-07-25 起放开电话与地址（原规则禁止，用户要求改）。适用于官网页面；**社媒视频/图文正文仍不得出现电话，那属于平台判定的站外引流**，见 `视频流水线/config/引流话术.json`。

## 三、禁用表述（任何页面不得出现）

> **判定总原则（2026-07-27 用户拍板，优先于本节所有条目）：看主语是谁。**
> 讲别人的案例、讲故事、举例说明、反面警示，**都没关系**；只有「宣传彤鼎在做这件事」才违规。
> - ❌「彤鼎多年港美股**操盘**经验」——在说我们做这个
> - ✅「某公司**市值拉升**被证监会处罚」——别人的案例
> - ✅「**最强**的财务异常信号是高增长低现金流」——形容客观现象，不是自夸
>
> 例外：①导流与联系方式不看主语，出现即违规；②广告法绝对化用语只在**自夸**时违规；③主语不明时按违规处理。
> 详见 `C:\TDGroupSEO\platform_rules.md` 8.2 开头，`compliance_gate.py` 已按此实现。

1. 无法验证的形容词：顶级、第一、最强、行业领先、著名、知名。
2. 知识文库文章正文中，不出现客户公司名称或股票代码，经验一律用行业维度表述。**例外（2026-07-25 扩大适用范围）**：官网**营销类页面**——首页、关于页、`club.html`、`legacy.html` 及后续同类落地页——的「案例 · Track Record」板块，可具名列示彤鼎团队主导及深度参与、且已在交易所公开挂牌的上市案例（含公司名与股票代码），但须同时满足——① 采用团队口径「彤鼎团队主导及深度参与的境外及A股上市项目，含团队以不同主体承做」；② 附合规免责（彤鼎为企业管理咨询机构、持牌业务由持牌合作机构承做、不构成证券发行或投资建议）；③ 只列已公开挂牌信息，不披露在辅导/问询中的未公开项目主体名称。**`articles/` 知识文库正文的第三方公司具名——2026-07-27 用户拍板放开，改为按风险分级**（此前是一刀切禁止）：

   - ✅ **放行**：公开可查的**事实型**叙述——架构设计、上市地选择、股东关系、已生效的司法判决、已公布的监管处罚等，属「这家公司是怎么做公司的」范畴。三个前提须**同时**满足：① 来源公开可查（判决书／监管公告／招股书／主流媒体报道），不是传闻或未证实消息；② 只陈述事实、不加贬损评价，不侵害名誉权；③ 不涉个股投资价值。
   - ❌ **仍然禁止**：
     - **股票代码**——任何情况。代码是个股标识符，是平台判定「财经内容」最直接的信号，收益远小于风险。
     - **任何个股投资价值表述**：股价、市值涨跌、估值高低、值不值得买、看好／看空。这是「拿钱进场」，会把内容从「企业经营」赛道推进「财经」赛道，而彤鼎明确不申请财经资质（见 `no-finance-qualification-downgrade` 决策）。
     - **客户公司名称**——保密义务，与本条放开**无关**，仍然一律不写。
     - **未公开信息**——在辅导／问询中的项目主体名称。
     - 负面事件具名时的**贬损性形容**：可写「被证监会处罚」「二审判决认定违法解除」，不可写「这家公司烂透了」「典型的黑心企业」。
   - **一句话自检**：这段是在讲「这家公司是怎么做公司的」，还是在讲「这只股票值不值得拿钱进场」？**前者放行，后者拦下。**
   - 拿不准时用匿名化（「一家在纳斯达克上市的中国连锁咖啡企业」），实证表明匿名化不损失传播力，见 `C:\TDGroupSEO\article_spec.md`「案例与叙事」节。
3. 保本、保收益、稳赚、承诺回报等一切收益承诺类词汇。
4. 编造的案例、数据或引用。宁缺毋假。
   - **权益价值标注（2026-07-25 放开）**：会员权益可标注参考价值（如「专属融资方案 价值 10 万元」「闭门会 单场价值数万元」），用于体现权益含金量。前提是该数字对应真实的单项服务对外报价，不得凭空拔高；表述用「价值」而非「原价/折后价」，避免构成《广告法》第八条意义上的价格标示。
5. 点仓、坐庄、拉盘、**操盘/操盘手/做市值/市值拉升/撬动市值增量/接票方**等操纵类词汇（合规科普中作反面警示除外，须明确标注违规）。
   **尤其禁止用于自述彤鼎的能力或服务**——`about.html` 已公开承诺"不参与任何形式的市场操纵、坐庄拉盘"，正文再自称"操盘/做市值"即构成自相矛盾。（2026-07-27 全库审计补入，同步 `C:\TDGroupSEO\platform_rules.md` 8.2 市场操纵嫌疑类。）
   **例外：个人履历中的职业身份保留**（2026-07-27 用户拍板）——"前加拿大券商**操盘手**"是描述过往任职岗位（券商交易员），可用；"彤鼎多年港美股**操盘**经验"是自述服务能力，禁用。判据：**"某人过去是什么岗位"→放行；"我们能对市值做什么"→拦下。**

## 四、五（文章生产标准 · 发布前合规闸 · 发布流程）

⮕ 已移至 **`.claude/rules/publishing.md`**（逐字保留）。写文章或发布前必读那一份。

## 六、修改边界

- 未经用户明确要求，不得改动：品牌文案、合规声明、联系方式、整体视觉风格（黑金配色 #c9a962 / #0a0908）。
- **全站设计层（2026-09-12 廖总「以人类设计师的眼光优化，要国际投行那种大气」）**：`css/site.css` 是 22 个主页面共用的设计系统（大留白、h1 72px/h2 44px 细体、标签正体大写不用斜体、细线分栏不用框、金色只用在标签/细线/主按钮），`css/nav.css` 是主页面 + 254 篇文章共用的导航（中文单语菜单、「服务」下拉、≤960px 汉堡菜单）。两份都在各页内嵌样式**之后**加载、只做覆盖。⛔ 新建页面必须带这两个 `<link>`（文章页只带 nav.css，它的米白正文不吃 site.css）；⛔ 改版式改这两份，别回到各页内嵌样式里逐页改；⛔ 每个桌面规则若各页内嵌媒体查询也设过同一属性，site.css 里必须补对应的 ≤960 / ≤640 规则，否则手机端会被桌面值盖掉。
- 不得删除或重写既有文章。
- 对 `index.html` 的结构性改动（新增/删除板块）需先向用户确认。
- **改过 `faq.html` 的可见问答后，必须执行 `python tools/rebuild_faq_schema.py`**。FAQPage schema 以可见 `<details>` 问答为唯一真源反向生成；只改可见文字而不同步 schema，会造成结构化数据与页面内容不符（属违规），且问题标题仍能对上、肉眼看不出来。校验用 `--check`（有差异退出码 1）。
- **从模板页复制生成新页面时，必须剥掉继承来的 JSON-LD 再写自己的**。2026-07-29 用 `pre-ipo-program.html` 做模板生成门槛速查页时，连带继承了描述「上市预备期陪跑」的 `Service` 与三条页面上根本不存在的 `FAQPage` 问答——schema 必须描述当前页面。
- **不得为了「显得新」而只改 `dateModified`**。schema 说本周更新、正文一个字没动，是搜索引擎明确打击的 content refresh spam，被识别出来比不更新更糟。内容时效复核走 `python tools/freshness_audit.py` 取队列、`python tools/bump_modified.py` 记账——后者以正文内容哈希为准 fail-closed，正文没变会拒绝改日期（核过确实无需改，用 `--reviewed-only`）。**不要绕过它手改 schema 日期。**这一班已并入 `weekly-review` 定时任务，不要新建同类任务。
- **站内助手 `js/assistant.js` 必须保持检索式，禁止改为调用生成式 API**。两个原因：① 本站为纯静态托管，任何 LLM 的 API key 都只能写在前端，等于公开泄露；② 彤鼎为金融咨询机构，生成式回答会即兴编造上市门槛、费用、收益等数字，构成合规风险。助手只能返回 `js/assistant-kb.json` 中来自本站已审核内容的原文。若确需生成式能力，必须先架设服务端代理（密钥留在服务端）并经用户确认，不得在前端直连。

## 七、初创客群业务线（2026-08-08 建立，长期方针）

⮕ 已移至 **`.claude/rules/line-startup.md`**（逐字保留）。

## 八、语言与语气

全站中文为主、英文摘要并行。行文专业、克制、直给结论，杜绝营销腔和夸张修辞。所有断言可验证，所有数字有依据。

## 九、分工原则：不要把活推给用户（2026-08-11 用户拍板）

用户不是程序员，也不该充当你的双手。**凡是能自动做的，自己做完；自己环境做不了的，先转给能做的那一方；只有物理上只有用户能做的，才找他。**

### 派活顺序（必须按此顺序判断，不许跳到最后一档）

1. **本会话能做的** —— 直接做完，不要先问"要不要我做"。
2. **本会话做不了、但**本机 Claude**能做的** —— 写成一段可直接转发的指令交给用户转交，或直接说明由本机会话接手。本机有磁盘、git 凭据、计划任务权限，云端够不着的活它基本都能干。
3. **只有用户本人能做的** —— 才交给他。这一档只包含三类：
   - **点击授权/登录**（GitHub 建库、会话来源授权、订阅、连接器授权）
   - **只有他知道的内容**（报价策略、客户判断、商务条件）
   - **拍板决策**（方向、取舍、对外承诺）

**判断题**：如果一件事「用户照着我给的命令粘贴执行」就能完成，那它属于第 2 档不是第 3 档——应该交给本机 Claude，而不是让用户当人肉终端。

### 开工前先验权限，别让用户一步步试错

**接到涉及新仓库、新服务、新连接器的任务，第一件事是验证本会话有没有权限，把结论一次说清。**

2026-08-11 的教训：给用户设计了「建私有仓库」方案，让他建了库、下载 bundle、装 Git、拖拽上传、又拖第二次、再粘贴 YAML——折腾一整轮之后才确认**云端会话对该私有仓库没有任何读写权限**（git 代理 403、GitHub API 拒绝、`add_repo` 需批准且中途失效）。这个结论本该在提出方案的那一刻就用一次工具调用验明，而不是让用户替我逐条试错。

**所以：先探边界，再给方案。方案里要写明哪一步由谁做。**

### 工作台目录（2026-08-11 用户指定）

本机统一工作目录：**`D:\彤鼎工作台`**。四个仓库并列放在它下面：

```
D:\彤鼎工作台\
├── CLAUDE.md              ← ⛔ 日常口径真源（约 9 万字，人工维护，**不是生成物**）
├── AGENTS.md              ← 别家 AI 的总入口（由 td-internal/tools/init_workspace.py 生成）
├── GEMINI.md / QWEN.md / CONVENTIONS.md / .cursorrules / .clinerules / .windsurfrules
│                          ← 同上，各厂商入口，内容与 AGENTS.md 一致
├── 官网仓库\               ← 官网 + 本规则总纲（仓库名是 TDgroupHK.github.io）
├── TDgroup\               ← 短视频线内容供给
├── td-internal\           ← 内部资料库（私有）
└── TDgroupHK\             ← GitHub 账号**公开主页 README**（不是历史仓库）
```

⚠ **`TDgroupHK` 不是「历史仓库」**：同名仓库就是 github.com/TDgroupHK 那个页面，
任何人都看得到，属于对外品牌门面，**要跟着官网一起保持新**。2026-08-13 发现它把知识文库
写成 148 篇（实际 199，少报 51 篇），从 07-26 一路烂着——根因就是文档把它标成
「历史仓库、一般不动」，于是没有任何人回头看它。官网内容有大改动时顺手核一眼那份 README。

⛔⛔ **`CLAUDE.md` 不在生成列表里，谁也不许拿它当入口文件覆盖。**
工作台根目录那份 `CLAUDE.md` 是**日常口径真源**（放权、发布纪律、报价、跟进线、定时班次），
每个会话与全部定时班次开场自动加载，而**工作台根目录不是 git 仓库，覆盖了没有任何地方找得回来**。
2026-08-13 差一步就出事：`init_workspace.py` 的生成清单里原本含 `CLAUDE.md`，
写它的是云端会话——没有磁盘访问权，只看得见「入口文件还不存在」，就当根目录是空的。
现已加闸：**只覆盖带本脚本生成标记的文件**，别人的东西一个字不碰。

⚠ **官网那个仓库在本机叫 `官网仓库`，不叫 `TDgroupHK.github.io`**——每天发文的流水线脚本
把这个路径写死了（18 处引用，其中 7 个是活的发文脚本）。目录名是随便叫的、发文线是活的，
所以改便宜的那头。⛔ **别按仓库名再克隆一份**：08-13 就这么冒出过两份，各自 pull、
停在不同提交上，而交接文档的自动快照读的是新那份、报出来的状态是旧的——**这种重复不报错，
两份看上去都很正常**。`restore.py` 已加等价名表：已经存在哪个就用哪个，绝不克隆出第二份。

⚠ **`TDgroup` 是稀疏检出**：`git ls-files` 报 30 个而工作区只有 17 个，这是正常的、不是缺文件。
排除掉的是 `publish/归档-成品视频备用-勿用/` 里 5 个共 37 MB 的废弃视频（普通 clone 会因此
反复 `early EOF` 拉不下来）。别去重新克隆，会再撞一次那堵墙。

**用户对任何 AI 只说一句「去接管 `D:\彤鼎工作台`」就该能全盘接手**，不需要再发任何指令。
这要求工作台根目录的入口文件始终存在且最新——由 `td-internal/tools/init_workspace.py` 生成，
`--check` 校验。**新增或修改规则后必须重跑它**，否则新来的 AI 读到的是旧版。

⚠ **但 Claude 读的是 `CLAUDE.md`，不读 `AGENTS.md`**——所以那套接管资料
（`td-internal/交接文档.md`、`restore.py`、对话存档）必须在工作台 `CLAUDE.md` 里点名，
否则对 Claude 完全隐形，而且零告警：文件都在、体检全绿，只是没有人会读到。
08-13 已在那份文件文首补了「⭐ 接管入口」一节。

### 三道 CI 闸（2026-08-11 建立，2026-09-15 增至三道，不可删）

规则光写在文档里没用，必须有东西执行。本仓库 `.github/workflows/` 下：

1. **`compliance-gate.yml`** —— 每次改 HTML 自动跑合规闸，不过就标红。
   兑现第四·补「推送前必跑合规闸」那条规则。此前该规则**没有任何东西执行**，
   2026-07-27 审计才发现「操盘/做市值」在站上挂了很久。
   ⚠️ 该闸依赖 `tools/compliance_gate.py` 与 `compliance_words.json`——目前只在
   本机 `C:\TDGroupSEO\`，**必须提交进本仓库 `tools/` 闸才真正生效**（缺失时闸只告警不拦截）。
   `platform_rules.md` 牵涉引流话术，留在私有仓库 `td-internal`，不要放进这个公开仓库。
2. **`publish-monitor.yml`** —— 每天北京时间 23:00 检查当天发了几篇，缺班开 issue。
   取代原先那条 Claude 云端定时任务，理由是不依赖任何 AI 账号。
3. **`rules-layering.yml`**（2026-09-15 建）—— 改动 `CLAUDE.md` 或 `.claude/rules/` 时自动跑
   `tools/check_rules.py`：核对拆分后一个字都没丢，并把常驻体积卡在 14,000 字符以内。
   加这道闸的理由与第 1 道相同——CLAUDE.md 从建立一路涨到 51 KB，就是因为「新规则写进哪份文件」
   这件事没有任何东西执行。⛔ 新增分线细则进 `.claude/rules/`，别再往常驻文件堆。

私有仓库 `td-internal` 另有两条：`backup-freshness.yml`（归档新鲜度）与
`daily-handover.yml`（每天自动刷新交接文档）。

### 云端会话碰不到私有仓库 `td-internal`

这是已知的结构性限制：云端会话的授权仓库集只含两个公开仓库。**要动 `td-internal`，走本机 Claude**，或在开会话时就把该仓库加进来源。

云端会话若产出了必须入库的内容（如对话归档），打包成 zip/bundle 交用户上传是**兜底手段，不是常规流程**——常规流程是这类活本来就该在本机做。

## 十、AI 转型线（2026-09-07 建立，长期方针）

⮕ 已移至 **`.claude/rules/line-ai.md`**（逐字保留）。

## 十一、资产型上市资本平台（2026-09-07 建立，长期方针）

⮕ 已移至 **`.claude/rules/line-asset.md`**（逐字保留）。

## 十二、电商税务合规线（2026-09-13 建立，长期方针）

⮕ 已移至 **`.claude/rules/line-ecommerce.md`**（逐字保留）。
